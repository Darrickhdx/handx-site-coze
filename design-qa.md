# Design QA — 个人主站 × 苏开元旗舰专题

日期：2026-07-21
审阅入口：`http://127.0.0.1:3217/`、`http://127.0.0.1:3217/sukaiyuan`

## 视觉与响应式

- 1280 × 900：个人首页与苏开元专题首屏通过；主导航、阅读导航、史料主视觉层级清楚。
- 768 × 1024：两页通过；布局正确转为单列／双列，无文字或卡片重叠。
- 390 × 844：两页及史料卡通过；页面无横向溢出，主标题、研究状态、年份记录与按钮可读。
- 移动菜单：默认只显示主导航，研究档案为二级折叠；Escape 可关闭并把焦点交还菜单按钮。
- 史料直达：`/archives#SRC-013` 能定位来源卡，并保留官方数字影印入口。
- 浏览器控制台：未发现错误日志。

## 内容与证据

- 1936 影印局部实际包含“留守司令蘇開元團長”原句；页面引文已改为可逐字对照的原文。
- 红线与放大镜明确标记为本站阅读辅助，不冒充原件批注。
- 文案明确区分家族记忆、文献同名记录与人物身份确认；1933、1936、1942 不被串成连续生平。
- 首页“知识网络”已改为“阅读导航”，6 个入口与实际配置一致；连线不表示人物关系或历史因果。

## 工程与边界

- TypeScript、ESLint、Stylelint、9 项数据协议测试与生产构建通过。
- HTTP 冒烟通过：11 个页面、4 个 JSON 端点、2 个登记静态资产、11 个唯一页面标题。
- 静态资产采用封闭清单和 SHA-256 校验；未登记资产会阻断构建／启动。
- 已撤下错误旧裁片与未授权 1937 图，并用 404 反向测试防止回归。
- `private/no-store`、`noindex/nofollow`、`robots.txt: Disallow /`、仅回环地址和禁止部署门禁均保持。

## 复核结论

- Codex：实现、构建、浏览器与边界总验收通过。
- Claude Opus：最终反方审查无 P0/P1，首版本地审阅可放行。
- 独立内容与技术审查：发现的引文失真、错图、资产越权、锚点、计数与移动菜单问题均已修复并复验。
- Grok：因工作区资料向外部模型导出受到安全策略阻断，本轮未形成其评审结论；不计入共识。

final result: passed

## V2 传播版复核（2026-07-24）

### 本轮范围

- 首页改为“家族史 × AI × 故事”的个人主站入口，并新增“发现”、三篇完整专题、小说试读、故事图谱和家族史工作室。
- 核心阅读路径为：封面故事 → 故事图谱 → 原件；研究后台仍保留在二级导航。
- 故事图谱默认打开“1936 平地泉”策展路线，首次进入即可看到节点边界和来源入口。

### 浏览器复核

- 1440 × 1000：`/`、`/discover`、`/discover/1936-pingdiquan`、`/novel`、`/graph`、`/studio` 通过。
- 768 × 1024：`/` 与 `/graph` 通过。
- 390 × 844：`/`、`/discover`、`/novel`、`/graph` 通过；无横向溢出、无真实坏图。
- 移动菜单可打开和关闭，主导航与研究档案入口可见。
- 故事图谱默认路线和节点详情正常，控制台无 warning／error。
- 截图保存在 `../../tmp/audit-2026-07-24/`，文件名以 `10-`、`20-`、`30-` 开头。

### 自动验收

- `pnpm validate`：PASS。
- `pnpm build`：PASS。
- `pnpm smoke:local`：PASS；17 个页面、4 个 JSON 端点、3 个登记静态资产、17 个唯一标题。
- 数据代际、资产 SHA、仅本机访问、`private/no-store`、`noindex/nofollow` 与禁止部署门禁均保持。

### 独立终审

- 本地传播体验：78/100；公开产品成熟度：30/100。
- 无 P0；史实边界和图片权利未发现越界。
- 主要缺口是站主公开身份、合规咨询入口和站外内容矩阵，均不适合由模型代填。
- Grok 反方终审认为，苏开元家族史是全站唯一不可被模板替代的 IP 锚点；但当前仍是“先见系统、不见作者”，上线前必须先补站主身份、隐私／联系规则与一份可核服务样例。
- 本轮 Fable 因用量额度不可用，LAN Opus 因授权过期不可用；未把它们计入共识。Codex 另行组织了网站成熟度、素材边界、故事内容和最终产品四个独立只读审查。

V2 final result: passed for local review; public release remains blocked.
